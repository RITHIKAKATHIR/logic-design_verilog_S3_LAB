module nor_behave(a,b,y);
input a,b;
output reg y;
always@(a,b)
begin 
 if(a==0&b==0)
  y=1;
else if(a==1&b==0)
 y=0;
 else if(a==0&b==1)
 y=0;
 else if(a==1&b==1)
 y=0;

 end
 endmodule 
 